const {Router} = require('express')
const router = Router();

  //test 

  router.get('/',(req,res)=>{
      res.status(200).json({msg:'im signed in'})
  })

module.exports = router;






































module.exports = router;